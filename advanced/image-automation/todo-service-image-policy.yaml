apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: todo-service
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: todo-service
  policy:
    alphabetical:
      order: asc
