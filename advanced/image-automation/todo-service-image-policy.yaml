apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: todo-service
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: todo-service
  filterTags:
    pattern: '^[0-9]+$'
  policy:
    numerical:
      order: asc
