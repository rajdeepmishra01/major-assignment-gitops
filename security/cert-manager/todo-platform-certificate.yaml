apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-platform-tls
  namespace: todo-platform
spec:
  secretName: todo-platform-tls
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - todo.local
